module github.com/devansh/feature-flag-engine/sdk

go 1.22

// No external dependencies — the SDK is pure stdlib.
// This is intentional: zero-dependency SDKs are easy to adopt.
// Users don't need to worry about transitive dependency conflicts.
